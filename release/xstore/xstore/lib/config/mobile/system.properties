dtv.util.sequence.SequenceFactory = dtv.cheetah.CheetahSequenceFactory
dtv.tenderauth.AuthFactory=dtv.cheetah.CheetahAuthFactory
dtv.hardware.IHardwareMgr=dtv.cheetah.hardware.CheetahHardwareMgr

# When true, prevents resolution-specific ui.properties from being loaded. This is necessary since
# the resolution that is considered is the server's monitor, which is completely useless for mobile.
dtv.pos.ui.properties.ignoreSpecificResolutions=true
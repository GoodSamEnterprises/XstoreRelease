<QuerySet xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../dtv/res/config/query/QueryConfig.xsd">
  
  <!-- ************************************************ -->
  <!--   this file has been automatically generated -->
  <!-- ************************************************ -->
  
  
  <!-- ************************************************ -->
  <!-- itm_attached_items <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_attached_items_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_attached_items p 
                    Where p.attached_item_id = c.attached_item_id
                      And p.organization_id = c.organization_id
                      And p.sold_item_id = c.sold_item_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_cross_reference <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_cross_reference_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_cross_reference p 
                    Where p.manufacturer_upc = c.manufacturer_upc
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_deal_prop <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_deal_prop_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_deal_prop p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.itm_deal_property_code = c.itm_deal_property_code
                      And p.effective_date = c.effective_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_dimension_type <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_dimension_type_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_dimension_type p 
                    Where p.organization_id = c.organization_id
                      And p.dimension_system = c.dimension_system
                      And p.dimension = c.dimension)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_dimension_value <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_dimension_value_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_dimension_value p 
                    Where p.organization_id = c.organization_id
                      And p.dimension_system = c.dimension_system
                      And p.dimension = c.dimension
                      And p.value = c.value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_images <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_images_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_images p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.feature_id = c.feature_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_label_batch <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_label_batch_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_label_batch p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.batch_name = c.batch_name
                      And p.item_id = c.item_id
                      And p.stock_label = c.stock_label)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_label_properties <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_label_properties_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_label_properties p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_msg <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_msg_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_msg p 
                    Where p.msg_id = c.msg_id
                      And p.organization_id = c.organization_id
                      And p.effective_datetime = c.effective_datetime)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_msg_cross_reference <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_msg_cross_reference_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_msg_cross_reference p 
                    Where p.msg_id = c.msg_id
                      And p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_msg_types <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_msg_types_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_msg_types p 
                    Where p.msg_id = c.msg_id
                      And p.organization_id = c.organization_id
                      And p.sale_lineitm_typcode = c.sale_lineitm_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_options <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_options_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_options p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_prices <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_prices_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_prices p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value
                      And p.itm_price_property_code = c.itm_price_property_code
                      And p.effective_date = c.effective_date
                      And p.price_qty = c.price_qty)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_prompt_properties <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_prompt_properties_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_prompt_properties p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.itm_prompt_property_code = c.itm_prompt_property_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_restrict_gs1 <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_restrict_gs1_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_restrict_gs1 p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.field_id = c.field_id
                      And p.start_value = c.start_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_restriction <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_restriction_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_restriction p 
                    Where p.organization_id = c.organization_id
                      And p.restriction_category = c.restriction_category
                      And p.restriction_code = c.restriction_code
                      And p.effective_date = c.effective_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_item_restrict_calendar <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_item_restrict_calendar_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_item_restrict_calendar p 
                    Where p.organization_id = c.organization_id
                      And p.restriction_category = c.restriction_category
                      And p.restriction_code = c.restriction_code
                      And p.day_code = c.day_code
                      And p.effective_date = c.effective_date
                      And p.start_time = c.start_time)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_kit_component <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_kit_component_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_kit_component p 
                    Where p.organization_id = c.organization_id
                      And p.kit_item_id = c.kit_item_id
                      And p.component_item_id = c.component_item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_matrix_sort_order <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_matrix_sort_order_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_matrix_sort_order p 
                    Where p.organization_id = c.organization_id
                      And p.matrix_sort_type = c.matrix_sort_type
                      And p.matrix_sort_id = c.matrix_sort_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_merch_hierarchy <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_merch_hierarchy_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_merch_hierarchy p 
                    Where p.hierarchy_id = c.hierarchy_id
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_non_phys_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_non_phys_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_non_phys_item p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_quick_items <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_quick_items_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_quick_items p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_refund_schedule <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_refund_schedule_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_refund_schedule p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.effective_date = c.effective_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_substitute_items <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_substitute_items_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_substitute_items p 
                    Where p.organization_id = c.organization_id
                      And p.primary_item_id = c.primary_item_id
                      And p.substitute_item_id = c.substitute_item_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_vendor <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_vendor_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_vendor p 
                    Where p.organization_id = c.organization_id
                      And p.vendor_id = c.vendor_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_warranty <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_warranty_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_warranty p 
                    Where p.organization_id = c.organization_id
                      And p.warranty_nbr = c.warranty_nbr
                      And p.warranty_typcode = c.warranty_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_warranty_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_warranty_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_warranty_item p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_warranty_item_xref <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_warranty_item_xref_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_warranty_item_xref p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.warranty_item_id = c.warranty_item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_warranty_item_price <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_warranty_item_price_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_warranty_item_price p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id
                      And p.warranty_price_seq = c.warranty_price_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- itm_warranty_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="itm_warranty_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From itm_warranty_journal p 
                    Where p.organization_id = c.organization_id
                      And p.warranty_nbr = c.warranty_nbr
                      And p.warranty_typcode = c.warranty_typcode
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_ar_sale_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_ar_sale_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_ar_sale_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_commission_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_commission_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_commission_mod p 
                    Where p.business_date = c.business_date
                      And p.commission_mod_seq_nbr = c.commission_mod_seq_nbr
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_correction_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_correction_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_correction_mod p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_coupon_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_coupon_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_coupon_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_cust_item_acct_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_cust_item_acct_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_cust_item_acct_mod p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_dimension_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_dimension_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_dimension_mod p 
                    Where p.business_date = c.business_date
                      And p.dimension_code = c.dimension_code
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_discount_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_discount_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_discount_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_escrow_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_escrow_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_escrow_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_invctl_document_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_invctl_document_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_invctl_document_mod p 
                    Where p.business_date = c.business_date
                      And p.invctl_document_mod_seq = c.invctl_document_mod_seq
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_kit_component_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_kit_component_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_kit_component_mod p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.component_item_id = c.component_item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_lineitm_assoc_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_lineitm_assoc_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_lineitm_assoc_mod p 
                    Where p.parent_business_date = c.parent_business_date
                      And p.lineitm_assoc_mod_seq = c.lineitm_assoc_mod_seq
                      And p.organization_id = c.organization_id
                      And p.parent_rtl_loc_id = c.parent_rtl_loc_id
                      And p.parent_rtrans_lineitm_seq = c.parent_rtrans_lineitm_seq
                      And p.parent_trans_seq = c.parent_trans_seq
                      And p.parent_wkstn_id = c.parent_wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_lineitm_assoc_typcode <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_lineitm_assoc_typcode_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_lineitm_assoc_typcode p 
                    Where p.lineitm_assoc_typcode = c.lineitm_assoc_typcode
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_inventory_loc_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_inventory_loc_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_inventory_loc_mod p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.mod_seq = c.mod_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_rtl_price_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_rtl_price_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_rtl_price_mod p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtl_price_mod_seq_nbr = c.rtl_price_mod_seq_nbr
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_deal_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_deal_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_deal_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_rtrans_flight_info <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_rtrans_flight_info_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_rtrans_flight_info p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_rtrans_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_rtrans_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_rtrans_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_lineitm_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_lineitm_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_lineitm_notes p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id
                      And p.note_seq = c.note_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_returned_item_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_returned_item_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_returned_item_count p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_returned_item_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_returned_item_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_returned_item_journal p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_sale_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_sale_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_sale_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_sale_tax_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_sale_tax_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_sale_tax_lineitm p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.sale_tax_lineitm_seq = c.sale_tax_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_tax_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_tax_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_tax_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_voucher_discount_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_voucher_discount_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_voucher_discount_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_voucher_sale_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_voucher_sale_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_voucher_sale_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>
  
  <!-- ************************************************ -->
  <!-- trl_warranty_modifier <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_warranty_modifier_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_warranty_modifier p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id
                      And p.warranty_modifier_seq = c.warranty_modifier_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_gift_registry_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_gift_registry_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_gift_registry_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_generic_lineitm_storage <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_generic_lineitm_storage_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_generic_lineitm_storage p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_no_sale_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_no_sale_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_no_sale_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_poslog_data <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_poslog_data_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_poslog_data p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_trans_link <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_trans_link_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_trans_link p 
                    Where p.business_date = c.business_date
                      And p.link_business_date = c.link_business_date
                      And p.link_rtl_loc_id = c.link_rtl_loc_id
                      And p.link_trans_seq = c.link_trans_seq
                      And p.link_wkstn_id = c.link_wkstn_id
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_post_void_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_post_void_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_post_void_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_raincheck <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_raincheck_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_raincheck p 
                    Where p.organization_id = c.organization_id
                      And p.rain_check_id = c.rain_check_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_raincheck_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_raincheck_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_raincheck_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_receipt_data <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_receipt_data_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_receipt_data p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.receipt_id = c.receipt_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_receipt_lookup <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_receipt_lookup_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_receipt_lookup p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.receipt_id = c.receipt_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_trans_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_trans_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_trans_notes p 
                    Where p.business_date = c.business_date
                      And p.note_seq = c.note_seq
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trn_trans_version <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trn_trans_version_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trn_trans_version p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- trl_rtrans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="trl_rtrans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From trl_rtrans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_xrtrans_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_xrtrans_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_xrtrans_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.line_seq = c.line_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_session <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_session_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_session p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.session_id = c.session_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_session_control_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_session_control_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_session_control_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_session_tndr <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_session_tndr_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_session_tndr p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.session_id = c.session_id
                      And p.tndr_id = c.tndr_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_session_wkstn <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_session_wkstn_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_session_wkstn p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.session_id = c.session_id
                      And p.session_wkstn_seq = c.session_wkstn_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_control_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_control_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_control_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_tndr_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_tndr_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_tndr_count p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_id = c.tndr_id
                      And p.tndr_typcode = c.tndr_typcode
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_denomination_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_denomination_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_denomination_count p 
                    Where p.business_date = c.business_date
                      And p.denomination_id = c.denomination_id
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_id = c.tndr_id
                      And p.tndr_typcode = c.tndr_typcode
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_repository <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_repository_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_repository p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_repository_id = c.tndr_repository_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_repository_float <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_repository_float_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_repository_float p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_repository_id = c.tndr_repository_id
                      And p.currency_id = c.currency_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_repository_status <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_repository_status_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_repository_status p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_repository_id = c.tndr_repository_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_serialized_tndr_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_serialized_tndr_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_serialized_tndr_count p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.serialized_tndr_count_seq = c.serialized_tndr_count_seq
                      And p.tndr_typcode = c.tndr_typcode
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_tndr_typcode_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_tndr_typcode_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_tndr_typcode_count p 
                    Where p.business_date = c.business_date
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.tndr_typcode = c.tndr_typcode
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_till_control_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_till_control_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_till_control_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tsn_till_ctrl_trans_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tsn_till_ctrl_trans_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tsn_till_ctrl_trans_detail p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.trans_lineitm_seq = c.trans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_acct_credit_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_acct_credit_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_acct_credit_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_ar_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_ar_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_ar_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_check_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_check_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_check_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_coupon_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_coupon_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_coupon_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_credit_debit_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_credit_debit_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_credit_debit_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_identity_verification <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_identity_verification_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_identity_verification p 
                    Where p.business_date = c.business_date
                      And p.identity_verification_seq = c.identity_verification_seq
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.trans_seq = c.trans_seq
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_send_check_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_send_check_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_send_check_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_tndr_auth_log <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_tndr_auth_log_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_tndr_auth_log p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.attempt_seq = c.attempt_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_signature <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_signature_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_signature p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_voucher <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_voucher_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_voucher p 
                    Where p.organization_id = c.organization_id
                      And p.serial_nbr = c.serial_nbr
                      And p.voucher_typcode = c.voucher_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_voucher_history <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_voucher_history_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_voucher_history p 
                    Where p.organization_id = c.organization_id
                      And p.serial_nbr = c.serial_nbr
                      And p.voucher_typcode = c.voucher_typcode
                      And p.history_seq = c.history_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ttr_voucher_tndr_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ttr_voucher_tndr_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ttr_voucher_tndr_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_award_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_award_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_award_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_card_nbr = c.cust_card_nbr
                      And p.acct_id = c.acct_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_award_acct_coupon <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_award_acct_coupon_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_award_acct_coupon p 
                    Where p.organization_id = c.organization_id
                      And p.cust_card_nbr = c.cust_card_nbr
                      And p.acct_id = c.acct_id
                      And p.coupon_id = c.coupon_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_charge_acct_history <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_charge_acct_history_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_charge_acct_history p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.history_seq = c.history_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_charge_acct_invoice <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_charge_acct_invoice_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_charge_acct_invoice p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.invoice_number = c.invoice_number)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_charge_acct_users <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_charge_acct_users_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_charge_acct_users p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.acct_user_id = c.acct_user_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_authorizations <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_authorizations_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_authorizations p 
                    Where p.organization_id = c.organization_id
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_acct_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_acct_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_acct_journal p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_acct_note <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_acct_note_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_acct_note p 
                    Where p.note_seq = c.note_seq
                      And p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_acct_plan <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_acct_plan_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_acct_plan p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.plan_id = c.plan_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_consumer_charge_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_consumer_charge_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_consumer_charge_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_item_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_item_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_item_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_item_acct_activity <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_item_acct_activity_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_item_acct_activity p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.cust_item_acct_detail_item_nbr = c.cust_item_acct_detail_item_nbr
                      And p.seq_nbr = c.seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_item_acct_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_item_acct_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_item_acct_detail p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.cust_item_acct_detail_item_nbr = c.cust_item_acct_detail_item_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_item_acct_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_item_acct_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_item_acct_journal p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_loyalty_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_loyalty_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_loyalty_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_card_nbr = c.cust_card_nbr
                      And p.cust_acct_id = c.cust_acct_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_cust_acct_card <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_cust_acct_card_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_cust_acct_card p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_card_nbr = c.cust_acct_card_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_delivery_modifier <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_delivery_modifier_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_delivery_modifier p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_escrow_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_escrow_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_escrow_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_escrow_acct_activity <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_escrow_acct_activity_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_escrow_acct_activity p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.seq_nbr = c.seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cat_payment_schedule <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cat_payment_schedule_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cat_payment_schedule p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cfg_user <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cfg_user_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cfg_user p 
                    Where p.user_name = c.user_name)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cfg_user_node <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cfg_user_node_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cfg_user_node p 
                    Where p.user_name = c.user_name
                      And p.org_scope = c.org_scope)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_address <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_address_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_address p 
                    Where p.organization_id = c.organization_id
                      And p.address_id = c.address_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_address_country <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_address_country_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_address_country p 
                    Where p.organization_id = c.organization_id
                      And p.country_id = c.country_id
                      And p.address_mode = c.address_mode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_address_postalcode <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_address_postalcode_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_address_postalcode p 
                    Where p.organization_id = c.organization_id
                      And p.country_id = c.country_id
                      And p.postal_code_id = c.postal_code_id
                      And p.address_mode = c.address_mode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_address_state <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_address_state_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_address_state p 
                    Where p.organization_id = c.organization_id
                      And p.country_id = c.country_id
                      And p.state_id = c.state_id
                      And p.address_mode = c.address_mode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_airport_zone <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_airport_zone_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_airport_zone p 
                    Where p.organization_id = c.organization_id
                      And p.zone_id = c.zone_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_airport_zone_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_airport_zone_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_airport_zone_detail p 
                    Where p.organization_id = c.organization_id
                      And p.zone_id = c.zone_id
                      And p.destination_zone_id = c.destination_zone_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_button_grid <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_button_grid_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_button_grid p 
                    Where p.organization_id = c.organization_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value
                      And p.grid_id = c.grid_id
                      And p.row_id = c.row_id
                      And p.column_id = c.column_id
                      And p.component_id = c.component_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_code_value <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_code_value_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_code_value p 
                    Where p.organization_id = c.organization_id
                      And p.category = c.category
                      And p.code = c.code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_country_return_map <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_country_return_map_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_country_return_map p 
                    Where p.organization_id = c.organization_id
                      And p.purchased_from = c.purchased_from
                      And p.return_to = c.return_to)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_translations <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_translations_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_translations p 
                    Where p.organization_id = c.organization_id
                      And p.locale = c.locale
                      And p.translation_key = c.translation_key)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_flight_info <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_flight_info_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_flight_info p 
                    Where p.organization_id = c.organization_id
                      And p.scheduled_date_time = c.scheduled_date_time
                      And p.flight_number = c.flight_number)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_reason_code <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_reason_code_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_reason_code p 
                    Where p.organization_id = c.organization_id
                      And p.reason_typcode = c.reason_typcode
                      And p.reason_code = c.reason_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_receipt_text <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_receipt_text_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_receipt_text p 
                    Where p.organization_id = c.organization_id
                      And p.text_code = c.text_code
                      And p.text_seq = c.text_seq
                      And p.text_subcode = c.text_subcode
                      And p.config_element = c.config_element)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_report_data <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_report_data_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_report_data p 
                    Where p.organization_id = c.organization_id
                      And p.owner_id = c.owner_id
                      And p.owner_type_enum = c.owner_type_enum
                      And p.report_id = c.report_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_report_lookup <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_report_lookup_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_report_lookup p 
                    Where p.organization_id = c.organization_id
                      And p.owner_id = c.owner_id
                      And p.owner_type_enum = c.owner_type_enum
                      And p.report_id = c.report_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_sequence <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_sequence_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_sequence p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.sequence_id = c.sequence_id
                      And p.sequence_mode = c.sequence_mode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_sequence_part <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_sequence_part_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_sequence_part p 
                    Where p.organization_id = c.organization_id
                      And p.sequence_id = c.sequence_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_shipping_cost <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_shipping_cost_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_shipping_cost p 
                    Where p.organization_id = c.organization_id
                      And p.category = c.category
                      And p.begin_range = c.begin_range
                      And p.end_range = c.end_range
                      And p.cost = c.cost)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_shipping_fee <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_shipping_fee_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_shipping_fee p 
                    Where p.rule_name = c.rule_name
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_shipping_fee_tier <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_shipping_fee_tier_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_shipping_fee_tier p 
                    Where p.rule_name = c.rule_name
                      And p.organization_id = c.organization_id
                      And p.parent_rule_name = c.parent_rule_name)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- com_trans_prompt_properties <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="com_trans_prompt_properties_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From com_trans_prompt_properties p 
                    Where p.organization_id = c.organization_id
                      And p.trans_prompt_property_code = c.trans_prompt_property_code
                      And p.effective_date = c.effective_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_customer_affiliation <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_customer_affiliation_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_customer_affiliation p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.cust_group_id = c.cust_group_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_customer_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_customer_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_customer_notes p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.note_seq = c.note_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_gift_registry_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_gift_registry_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_gift_registry_journal p 
                    Where p.organization_id = c.organization_id
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party_cross_reference <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_cross_reference_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party_cross_reference p 
                    Where p.child_party_id = c.child_party_id
                      And p.organization_id = c.organization_id
                      And p.parent_party_id = c.parent_party_id
                      And p.party_relationship_typcode = c.party_relationship_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party_email <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_email_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party_email p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.email_sequence = c.email_sequence)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party_id_xref <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_id_xref_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party_id_xref p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.alternate_id_owner = c.alternate_id_owner)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party_locale_information <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_locale_information_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party_locale_information p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.party_locale_seq = c.party_locale_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- crm_party_telephone <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="crm_party_telephone_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From crm_party_telephone p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.telephone_type = c.telephone_type)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_cheetah_device_access <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_cheetah_device_access_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_cheetah_device_access p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.token = c.token)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_dataloader_failure <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_dataloader_failure_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_dataloader_failure p 
                    Where p.organization_id = c.organization_id
                      And p.file_name = c.file_name
                      And p.run_timestamp = c.run_timestamp
                      And p.failure_seq = c.failure_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_dataloader_summary <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_dataloader_summary_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_dataloader_summary p 
                    Where p.organization_id = c.organization_id
                      And p.file_name = c.file_name
                      And p.run_timestamp = c.run_timestamp)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_device_registration <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_device_registration_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_device_registration p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_event_log <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_event_log_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_event_log p )]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- ctl_version_history <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="ctl_version_history_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From ctl_version_history p 
                    Where p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_category_service_loc <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_category_service_loc_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_category_service_loc p 
                    Where p.organization_id = c.organization_id
                      And p.service_loc_id = c.service_loc_id
                      And p.category_id = c.category_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_invoice <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_invoice_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_invoice p 
                    Where p.organization_id = c.organization_id
                      And p.service_loc_id = c.service_loc_id
                      And p.invoice_number = c.invoice_number)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_invoice_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_invoice_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_invoice_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.service_loc_id = c.service_loc_id
                      And p.invoice_number = c.invoice_number
                      And p.invoice_lineitm_seq = c.invoice_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_service_loc <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_service_loc_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_service_loc p 
                    Where p.organization_id = c.organization_id
                      And p.service_loc_id = c.service_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_task <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_task_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_task p 
                    Where p.organization_id = c.organization_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_item p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.work_item_seq = c.work_item_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_order_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_order_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_order_acct p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_order_acct_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_order_acct_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_order_acct_journal p 
                    Where p.organization_id = c.organization_id
                      And p.cust_acct_id = c.cust_acct_id
                      And p.cust_acct_code = c.cust_acct_code
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_order_category <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_order_category_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_order_category p 
                    Where p.organization_id = c.organization_id
                      And p.category_id = c.category_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_order_line_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_order_line_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_order_line_item p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_price_code <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_price_code_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_price_code p 
                    Where p.organization_id = c.organization_id
                      And p.price_code = c.price_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- cwo_work_order_pricing <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="cwo_work_order_pricing_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From cwo_work_order_pricing p 
                    Where p.organization_id = c.organization_id
                      And p.price_code = c.price_code
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- doc_document <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="doc_document_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From doc_document p 
                    Where p.document_id = c.document_id
                      And p.organization_id = c.organization_id
                      And p.document_type = c.document_type
                      And p.series_id = c.series_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- doc_document_definition <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="doc_document_definition_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From doc_document_definition p 
                    Where p.series_id = c.series_id
                      And p.organization_id = c.organization_id
                      And p.document_type = c.document_type)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- doc_document_def_properties <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="doc_document_def_properties_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From doc_document_def_properties p 
                    Where p.series_id = c.series_id
                      And p.organization_id = c.organization_id
                      And p.document_type = c.document_type
                      And p.doc_seq_nbr = c.doc_seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- doc_document_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="doc_document_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From doc_document_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_coupon_xref <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_coupon_xref_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_coupon_xref p 
                    Where p.coupon_serial_nbr = c.coupon_serial_nbr
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_discount <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_discount_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_discount p 
                    Where p.discount_code = c.discount_code
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_discount_compatibility <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_discount_compatibility_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_discount_compatibility p 
                    Where p.compatible_discount_code = c.compatible_discount_code
                      And p.organization_id = c.organization_id
                      And p.primary_discount_code = c.primary_discount_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_discount_group_mapping <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_discount_group_mapping_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_discount_group_mapping p 
                    Where p.cust_group_id = c.cust_group_id
                      And p.discount_code = c.discount_code
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_discount_item_exclusions <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_discount_item_exclusions_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_discount_item_exclusions p 
                    Where p.discount_code = c.discount_code
                      And p.item_id = c.item_id
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_discount_item_inclusions <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_discount_item_inclusions_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_discount_item_inclusions p 
                    Where p.discount_code = c.discount_code
                      And p.item_id = c.item_id
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- dsc_disc_type_eligibility <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="dsc_disc_type_eligibility_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From dsc_disc_type_eligibility p 
                    Where p.discount_code = c.discount_code
                      And p.organization_id = c.organization_id
                      And p.sale_lineitm_typcode = c.sale_lineitm_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_answers <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_answers_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_answers p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.challenge_code = c.challenge_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_fingerprint <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_fingerprint_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_fingerprint p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.fingerprint_seq = c.fingerprint_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_message <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_message_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_message p 
                    Where p.organization_id = c.organization_id
                      And p.message_id = c.message_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_notes p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.note_seq = c.note_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_password <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_password_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_password p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.password_seq = c.password_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_store <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_store_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_store p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.employee_store_seq = c.employee_store_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_task <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_task_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_task p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.task_id = c.task_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_employee_task_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_employee_task_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_employee_task_notes p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.task_id = c.task_id
                      And p.note_seq = c.note_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- hrs_work_codes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="hrs_work_codes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From hrs_work_codes p 
                    Where p.organization_id = c.organization_id
                      And p.work_code = c.work_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_carton <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_carton_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_carton p 
                    Where p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.carton_id = c.carton_id
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_inventory_loc_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_inventory_loc_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_inventory_loc_mod p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.document_id = c.document_id
                      And p.document_typcode = c.document_typcode
                      And p.document_line_nbr = c.document_line_nbr
                      And p.mod_seq = c.mod_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_document_lineitm_note <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_document_lineitm_note_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_document_lineitm_note p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.note_id = c.note_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_document_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_document_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_document_notes p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.note_id = c.note_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_stock_fiscal_year <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_stock_fiscal_year_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_stock_fiscal_year p 
                    Where p.organization_id = c.organization_id
                      And p.fiscal_year = c.fiscal_year)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_bucket <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_bucket_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_bucket p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.bucket_id = c.bucket_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_document <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_document_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_document p 
                    Where p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_document_xref <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_document_xref_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_document_xref p 
                    Where p.organization_id = c.organization_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.document_typcode = c.document_typcode
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_document_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_document_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_document_lineitm p 
                    Where p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_trans_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_trans_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_trans_detail p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.invctl_trans_seq = c.invctl_trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_cst_item_yearend <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_cst_item_yearend_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_cst_item_yearend p 
                    Where p.organization_id = c.organization_id
                      And p.fiscal_year = c.fiscal_year
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_count_id = c.inv_count_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_bucket <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_bucket_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_bucket p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_count_id = c.inv_count_id
                      And p.inv_bucket_id = c.inv_bucket_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_section <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_section_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_section p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_bucket_id = c.inv_bucket_id
                      And p.section_id = c.section_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_section_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_section_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_section_detail p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_bucket_id = c.inv_bucket_id
                      And p.section_id = c.section_id
                      And p.section_detail_nbr = c.section_detail_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_sheet <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_sheet_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_sheet p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_count_id = c.inv_count_id
                      And p.count_sheet_nbr = c.count_sheet_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_sheet_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_sheet_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_sheet_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_count_id = c.inv_count_id
                      And p.count_sheet_nbr = c.count_sheet_nbr
                      And p.lineitm_nbr = c.lineitm_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_count_snapshot <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_count_snapshot_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_count_snapshot p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_count_id = c.inv_count_id
                      And p.inv_location_id = c.inv_location_id
                      And p.inv_bucket_id = c.inv_bucket_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_invctl_doc_lineserial <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_invctl_doc_lineserial_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_invctl_doc_lineserial p 
                    Where p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.serial_line_nbr = c.serial_line_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_item_acct_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_item_acct_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_item_acct_mod p 
                    Where p.organization_id = c.organization_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.document_typcode = c.document_typcode
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_inventory_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_inventory_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_inventory_journal p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.trans_lineitm_seq = c.trans_lineitm_seq
                      And p.journal_seq = c.journal_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_location <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_location_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_location p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_location_id = c.inv_location_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_location_availability <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_location_availability_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_location_availability p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.location_id = c.location_id
                      And p.availability_code = c.availability_code)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_location_bucket <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_location_bucket_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_location_bucket p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.location_id = c.location_id
                      And p.bucket_id = c.bucket_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_movement_pending <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_movement_pending_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_movement_pending p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.trans_lineitm_seq = c.trans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_movement_pending_detail <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_movement_pending_detail_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_movement_pending_detail p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.trans_lineitm_seq = c.trans_lineitm_seq
                      And p.pending_seq = c.pending_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_rep_document_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_rep_document_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_rep_document_lineitm p 
                    Where p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.invctl_document_line_nbr = c.invctl_document_line_nbr
                      And p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>
  
  <!-- ************************************************ -->
  <!-- inv_sum_count_trans_dtl <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_sum_count_trans_dtl_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_sum_count_trans_dtl p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.business_date = c.business_date
                      And p.trans_seq = c.trans_seq
                      And p.trans_line_seq = c.trans_line_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_valid_destinations <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_valid_destinations_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_valid_destinations p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.document_typcode = c.document_typcode
                      And p.document_subtypcode = c.document_subtypcode
                      And p.destination_type_enum = c.destination_type_enum
                      And p.destination_id = c.destination_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_mptrans_lineitm <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_mptrans_lineitm_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_mptrans_lineitm p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.trans_lineitm_seq = c.trans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_serialized_stock_ledger <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_serialized_stock_ledger_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_serialized_stock_ledger p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.inv_location_id = c.inv_location_id
                      And p.item_id = c.item_id
                      And p.serial_nbr = c.serial_nbr
                      And p.bucket_id = c.bucket_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_shipment <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_shipment_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_shipment p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.shipment_seq = c.shipment_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_shipment_address <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_shipment_address_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_shipment_address p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.shipment_seq = c.shipment_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_shipment_lines <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_shipment_lines_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_shipment_lines p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.invctl_document_id = c.invctl_document_id
                      And p.document_typcode = c.document_typcode
                      And p.shipment_seq = c.shipment_seq
                      And p.lineitm_seq = c.lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_shipper <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_shipper_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_shipper p 
                    Where p.organization_id = c.organization_id
                      And p.shipper_id = c.shipper_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_shipper_method <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_shipper_method_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_shipper_method p 
                    Where p.organization_id = c.organization_id
                      And p.shipper_method_id = c.shipper_method_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- inv_stock_ledger_acct <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="inv_stock_ledger_acct_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From inv_stock_ledger_acct p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.bucket_id = c.bucket_id
                      And p.inv_location_id = c.inv_location_id
                      And p.item_id = c.item_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_close_dates <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_close_dates_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_close_dates p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.close_date = c.close_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_closing_message <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_closing_message_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_closing_message p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_cycle_questions <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_cycle_questions_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_cycle_questions p 
                    Where p.organization_id = c.organization_id
                      And p.question_id = c.question_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_cycle_question_answers <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_cycle_question_answers_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_cycle_question_answers p 
                    Where p.answer_id = c.answer_id
                      And p.answer_timestamp = c.answer_timestamp
                      And p.organization_id = c.organization_id
                      And p.question_id = c.question_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_cycle_question_choices <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_cycle_question_choices_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_cycle_question_choices p 
                    Where p.answer_id = c.answer_id
                      And p.organization_id = c.organization_id
                      And p.question_id = c.question_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_org_hierarchy <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_org_hierarchy_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_org_hierarchy p 
                    Where p.organization_id = c.organization_id
                      And p.org_code = c.org_code
                      And p.org_value = c.org_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_pricing_hierarchy <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_pricing_hierarchy_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_pricing_hierarchy p 
                    Where p.organization_id = c.organization_id
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_rtl_loc <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_rtl_loc_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_rtl_loc p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_state_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_state_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_state_journal p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.status_typcode = c.status_typcode
                      And p.state_journal_id = c.state_journal_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- loc_wkstn <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="loc_wkstn_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From loc_wkstn p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_cust_groups <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_cust_groups_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_cust_groups p 
                    Where p.deal_id = c.deal_id
                      And p.organization_id = c.organization_id
                      And p.cust_group_id = c.cust_group_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_document_xref <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_document_xref_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_document_xref p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id
                      And p.series_id = c.series_id
                      And p.document_type = c.document_type)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_field_test <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_field_test_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_field_test p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id
                      And p.item_ordinal = c.item_ordinal
                      And p.item_condition_group = c.item_condition_group
                      And p.item_condition_seq = c.item_condition_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_item p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id
                      And p.item_ordinal = c.item_ordinal)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_trig <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_trig_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_trig p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id
                      And p.deal_trigger = c.deal_trigger)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- prc_deal_week <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="prc_deal_week_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From prc_deal_week p 
                    Where p.organization_id = c.organization_id
                      And p.deal_id = c.deal_id
                      And p.day_code = c.day_code
                      And p.start_time = c.start_time)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- rpt_organizer <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="rpt_organizer_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From rpt_organizer p 
                    Where p.organization_id = c.organization_id
                      And p.report_name = c.report_name
                      And p.report_group = c.report_group
                      And p.report_element = c.report_element)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sch_emp_time_off <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sch_emp_time_off_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sch_emp_time_off p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.time_off_seq = c.time_off_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sch_schedule <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sch_schedule_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sch_schedule p 
                    Where p.organization_id = c.organization_id
                      And p.employee_id = c.employee_id
                      And p.business_date = c.business_date
                      And p.schedule_seq = c.schedule_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sch_shift <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sch_shift_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sch_shift p 
                    Where p.organization_id = c.organization_id
                      And p.shift_id = c.shift_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_acl <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_acl_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_acl p 
                    Where p.organization_id = c.organization_id
                      And p.secured_object_id = c.secured_object_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_access_types <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_access_types_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_access_types p 
                    Where p.access_typcode = c.access_typcode
                      And p.organization_id = c.organization_id
                      And p.secured_object_id = c.secured_object_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_groups <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_groups_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_groups p 
                    Where p.group_id = c.group_id
                      And p.organization_id = c.organization_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_privilege <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_privilege_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_privilege p 
                    Where p.organization_id = c.organization_id
                      And p.privilege_type = c.privilege_type)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_activity_log <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_activity_log_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_activity_log p )]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_user_password <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_user_password_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_user_password p 
                    Where p.organization_id = c.organization_id
                      And p.username = c.username
                      And p.password_seq = c.password_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sec_user_role <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sec_user_role_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sec_user_role p 
                    Where p.organization_id = c.organization_id
                      And p.user_role_id = c.user_role_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- sls_sales_goal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="sls_sales_goal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From sls_sales_goal p 
                    Where p.organization_id = c.organization_id
                      And p.sales_goal_id = c.sales_goal_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_postal_code_mapping <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_postal_code_mapping_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_postal_code_mapping p 
                    Where p.city = c.city
                      And p.organization_id = c.organization_id
                      And p.postal_code = c.postal_code
                      And p.tax_loc_id = c.tax_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_rtl_loc_tax_mapping <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_rtl_loc_tax_mapping_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_rtl_loc_tax_mapping p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_authority <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_authority_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_authority p 
                    Where p.organization_id = c.organization_id
                      And p.tax_authority_id = c.tax_authority_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_bracket <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_bracket_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_bracket p 
                    Where p.organization_id = c.organization_id
                      And p.tax_bracket_id = c.tax_bracket_id
                      And p.tax_bracket_seq_nbr = c.tax_bracket_seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_code <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_code_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_code p 
                    Where p.organization_id = c.organization_id
                      And p.tax_code_id = c.tax_code_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_exemption <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_exemption_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_exemption p 
                    Where p.organization_id = c.organization_id
                      And p.tax_exemption_id = c.tax_exemption_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_group <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_group_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_group p 
                    Where p.organization_id = c.organization_id
                      And p.tax_group_id = c.tax_group_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_group_rule <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_group_rule_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_group_rule p 
                    Where p.organization_id = c.organization_id
                      And p.tax_group_id = c.tax_group_id
                      And p.tax_loc_id = c.tax_loc_id
                      And p.tax_rule_seq_nbr = c.tax_rule_seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_loc <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_loc_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_loc p 
                    Where p.organization_id = c.organization_id
                      And p.tax_loc_id = c.tax_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_rate_rule <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_rate_rule_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_rate_rule p 
                    Where p.organization_id = c.organization_id
                      And p.tax_group_id = c.tax_group_id
                      And p.tax_loc_id = c.tax_loc_id
                      And p.tax_rate_rule_seq = c.tax_rate_rule_seq
                      And p.tax_rule_seq_nbr = c.tax_rule_seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_rate_rule_override <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_rate_rule_override_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_rate_rule_override p 
                    Where p.expr_datetime = c.expr_datetime
                      And p.organization_id = c.organization_id
                      And p.tax_group_id = c.tax_group_id
                      And p.tax_loc_id = c.tax_loc_id
                      And p.tax_rate_rule_seq = c.tax_rate_rule_seq
                      And p.tax_rule_seq_nbr = c.tax_rule_seq_nbr)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tax_tax_group_mapping <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tax_tax_group_mapping_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tax_tax_group_mapping p 
                    Where p.organization_id = c.organization_id
                      And p.customer_group_id = c.customer_group_id
                      And p.tax_group_id = c.tax_group_id
                      And p.rtl_loc_id = c.rtl_loc_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_payroll <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_payroll_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_payroll p 
                    Where p.rtl_loc_id = c.rtl_loc_id
                      And p.party_id = c.party_id
                      And p.organization_id = c.organization_id
                      And p.business_date = c.business_date
                      And p.payroll_category = c.payroll_category)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_payroll_category <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_payroll_category_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_payroll_category p 
                    Where p.organization_id = c.organization_id
                      And p.payroll_category = c.payroll_category)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_payroll_header <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_payroll_header_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_payroll_header p 
                    Where p.rtl_loc_id = c.rtl_loc_id
                      And p.party_id = c.party_id
                      And p.organization_id = c.organization_id
                      And p.week_ending_date = c.week_ending_date)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_payroll_notes <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_payroll_notes_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_payroll_notes p 
                    Where p.organization_id = c.organization_id
                      And p.party_id = c.party_id
                      And p.week_ending_date = c.week_ending_date
                      And p.note_seq = c.note_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_timecard_entry <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_timecard_entry_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_timecard_entry p 
                    Where p.organization_id = c.organization_id
                      And p.business_date = c.business_date
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.party_id = c.party_id
                      And p.timecard_entry_id = c.timecard_entry_id
                      And p.wkstn_id = c.wkstn_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_timecard_entry_comment <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_timecard_entry_comment_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_timecard_entry_comment p 
                    Where p.organization_id = c.organization_id
                      And p.week_ending_date = c.week_ending_date
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.wkstn_id = c.wkstn_id
                      And p.party_id = c.party_id
                      And p.comment_seq = c.comment_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_timecard_journal <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_timecard_journal_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_timecard_journal p 
                    Where p.organization_id = c.organization_id
                      And p.business_date = c.business_date
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.party_id = c.party_id
                      And p.timecard_entry_id = c.timecard_entry_id
                      And p.wkstn_id = c.wkstn_id
                      And p.timecard_entry_seq = c.timecard_entry_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- thr_timeclk_trans <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="thr_timeclk_trans_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From thr_timeclk_trans p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr p 
                    Where p.organization_id = c.organization_id
                      And p.tndr_id = c.tndr_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr_availability <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_availability_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr_availability p 
                    Where p.availability_code = c.availability_code
                      And p.organization_id = c.organization_id
                      And p.tndr_id = c.tndr_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr_denomination <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_denomination_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr_denomination p 
                    Where p.denomination_id = c.denomination_id
                      And p.organization_id = c.organization_id
                      And p.tndr_id = c.tndr_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_exchange_rate <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_exchange_rate_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_exchange_rate p 
                    Where p.organization_id = c.organization_id
                      And p.base_currency = c.base_currency
                      And p.target_currency = c.target_currency
                      And p.level_code = c.level_code
                      And p.level_value = c.level_value)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr_options <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_options_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr_options p 
                    Where p.organization_id = c.organization_id
                      And p.tndr_id = c.tndr_id
                      And p.config_element = c.config_element)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr_typcode <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_typcode_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr_typcode p 
                    Where p.organization_id = c.organization_id
                      And p.tndr_typcode = c.tndr_typcode)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- tnd_tndr_user_settings <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="tnd_tndr_user_settings_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From tnd_tndr_user_settings p 
                    Where p.group_id = c.group_id
                      And p.organization_id = c.organization_id
                      And p.tndr_id = c.tndr_id
                      And p.usage_code = c.usage_code
                      And p.entry_mthd_code = c.entry_mthd_code
                      And p.config_element = c.config_element)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_address_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_address_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_address_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.address_seq = c.address_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_balance_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_balance_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_balance_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq
                      And p.mod_seq = c.mod_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_customer_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_customer_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_customer_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_customization_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_customization_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_customization_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq
                      And p.mod_seq = c.mod_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_fee_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_fee_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_fee_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq
                      And p.mod_seq = c.mod_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_fulfillment_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_fulfillment_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_fulfillment_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_item_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_item_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_item_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_order <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_order_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_order p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_order_fee <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_order_fee_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_order_fee p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_order_line <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_order_line_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_order_line p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_order_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_order_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_order_mod p 
                    Where p.organization_id = c.organization_id
                      And p.rtl_loc_id = c.rtl_loc_id
                      And p.business_date = c.business_date
                      And p.wkstn_id = c.wkstn_id
                      And p.trans_seq = c.trans_seq
                      And p.rtrans_lineitm_seq = c.rtrans_lineitm_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_order_payment <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_order_payment_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_order_payment p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.sequence = c.sequence)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xom_source_mod <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xom_source_mod_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xom_source_mod p 
                    Where p.organization_id = c.organization_id
                      And p.order_id = c.order_id
                      And p.detail_seq = c.detail_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xunit_result <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xunit_result_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xunit_result p 
                    Where p.organization_id = c.organization_id
                      And p.hostname = c.hostname
                      And p.result_timestamp = c.result_timestamp)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>

  <!-- ************************************************ -->
  <!-- xunit_result_item <<< *                                   -->
  <!-- ************************************************ -->
  <Query name="xunit_result_item_prop.child">
    <QueryHandler dtype="Class">dtv.data2.access.query.SqlQueryHandler</QueryHandler>
    <SQL>
      <Statement dtype="String"><![CDATA[
        DELETE {%ALIAS%} FROM {%TABLE%} c
          WHERE organization_id = ?
            AND NOT EXISTS
                  (Select p.* From xunit_result_item p 
                    Where p.organization_id = c.organization_id
                      And p.hostname = c.hostname
                      And p.result_timestamp = c.result_timestamp
                      And p.result_item_seq = c.result_item_seq)]]>
      </Statement>
      <Parameter name="argOrganizationId" />
    </SQL>
  </Query>
</QuerySet>

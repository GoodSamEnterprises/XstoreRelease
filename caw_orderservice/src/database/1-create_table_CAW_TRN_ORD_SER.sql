CREATE TABLE CAW_TRN_ORD_SER(
	TRANS_SEQ NUMBER(19,0) NOT NULL,
	RTL_LOC_ID NUMBER(10,0) NOT NULL,
	WKSTN_ID NUMBER(19,0) NOT NULL,
	BUSINESS_DATE TIMESTAMP(6) NOT NULL,
	STATUS NUMBER(1,0),
	RESPONSE_CODE NUMBER(19,0),
	RESPONSE_MESSAGE CLOB,
	RESPONSE_DESC CLOB,
	CREATE_DATE TIMESTAMP(6),
	UPDATE_DATE VARCHAR2(100 BYTE),
	RETRY_COUNT NUMBER(19,0)
);

Alter table CAW_TRN_ORD_SER  add CONSTRAINT "PK_CAW_ORDERS" PRIMARY KEY(TRANS_SEQ, WKSTN_ID, RTL_LOC_ID, BUSINESS_DATE)

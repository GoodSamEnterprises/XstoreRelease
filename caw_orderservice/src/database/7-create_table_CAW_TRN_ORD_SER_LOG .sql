CREATE TABLE CAW_TRN_ORD_SER_LOG (
        TIMESTAMP          VARCHAR2(255) NOT NULL,
        TRANS_SEQ          NUMBER(19,0) NOT NULL,
        RTL_LOC_ID         NUMBER(10,0) NOT NULL,
        WKSTN_ID           NUMBER(19,0) NOT NULL,
        BUSINESS_DATE      TIMESTAMP,
        REQUEST_MESSAGE    CLOB,
        RESPONSE_MESSAGE   CLOB,
        RESPONSE_CODE      VARCHAR2(255)
    );
    
Alter table CAW_TRN_ORD_SER_LOG  add CONSTRAINT "PK_CAW_TRN_ORD_SER_LOG" PRIMARY KEY(TIMESTAMP);
